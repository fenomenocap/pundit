import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Club Season Calibration",
};

export default function ClubSeasonEvaluationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
