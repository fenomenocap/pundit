import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "WC 2026 Evaluation",
};

export default function WcEvaluationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
